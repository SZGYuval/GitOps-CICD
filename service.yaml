apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: python-app
spec:
  selector:
    app: python
  type: NodePort
  ports:
    - targetPort: 5000
      port: 5000
      nodePort: 30005
      protocol: TCP